style: ruff 全绿收尾（9 → 0）

接上一个提交（503 → 9），清掉剩余 9 条：

- src/providers/__init__.py：删除未使用的 `is_mock_mode` 导入
  （已核实无任何消费者——`api/main.py` 等一律从 `src.core.config` 直接导入）
- tests/test_agents/test_coordinator.py：`d1` / `d2` 未使用，改为直接调用
  （仍保留两次 `_mock_decision()` 以推进 workflow_index，只去掉赋值）
- tests/test_chat/test_session_ttl.py：`running` 未使用，去掉赋值保留 `_create` 副作用
- tests/test_harness/test_checkpoint_durability.py：`stats` 未使用，去掉赋值保留 await
- tests/test_harness/test_integration.py：`warnings` 未使用，删除
- tests/test_harness/test_load.py：`now` 未使用，连带清理因此失效的 `import time`
- tests/test_harness/test_style_store.py：`entry` 未使用，去掉赋值保留 `_create` 落库副作用
- tests/test_workflow/test_manual.py：`done` 未使用，去掉赋值保留 await

原则：凡是被调用者有副作用的（推进状态、落库、清理），只去掉赋值、不删调用。

验证（与 CI 完全对齐）：
- `python -m ruff check src tests` → All checks passed
- `python -m pytest -m "not real" -q` → 1668 passed（含 slow）
